<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= title %></title>
  </head>
  <body>
   
    <h1><%= title %></h1>
   <p>
    <a href="/books/new">Add a book</a>
   </p>
    <% if (books.length) { %>
    <h2>These are the books!</h2>
    <ul>
      <% books.forEach(book => { %>
      <li>
        <a href="books/<%= book._id %>"><%= book.title %></a>
        <%= book.read ? "read 😋" : "not read 🤔" %>
      </li>
      <a href="/books/<%= book._id %>/edit">Edit this book!</a>
      <form 
      action= "/books/<%= book._id %>?_method=DELETE" method="POST">
      <button type="submit">
         Delete this book 💀
      </button>
     </form>
      <% }) %>
    </ul>
    <% } else { %>
    <h2>There are no books yet!</h2>
    <% } %>
      
    

  </body>
</html>